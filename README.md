# spamclassifier_Logit
This is a spam classifier developed in MATLAB using UCI repository(link mentioned in readme). It uses to LR model to classify message. And it's score is 80% correctly classified
